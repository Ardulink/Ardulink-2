coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

  ignore:
    - "ardulink-swing/**"
    - "ardulink-link-*/**"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
